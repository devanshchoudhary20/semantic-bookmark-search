import "~style.css"

// Scaffold placeholder — the real popup (import → embed → search → open) lands in Phase 4.
function IndexPopup() {
  return (
    <div className="flex h-[560px] w-[400px] items-center justify-center bg-white text-slate-500">
      <p className="text-sm">Semantic Bookmark Search — scaffold</p>
    </div>
  )
}

export default IndexPopup
